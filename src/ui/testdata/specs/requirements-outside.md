<!-- Requirements outside the `## Requirements` section. openspec's own
     message: "Main specs only parse requirements inside that section, so this
     requirement is currently invisible to validate, list, and archive." -->
# scattered Specification

## Purpose
Exists to hold a requirement that openspec cannot see.

### Requirement: Above the section
The system SHALL do this, where nothing can read it.

#### Scenario: Unreachable
- **WHEN** asked
- **THEN** done

## Requirements

### Requirement: Inside the section
The system SHALL do this too.

#### Scenario: Reachable
- **WHEN** asked
- **THEN** done

## Notes

### Requirement: Below the section
Also unreachable.

#### Scenario: Also unreachable
- **WHEN** asked
- **THEN** done
