<!-- A level-four heading with nothing under it. openspec does not count it:
     `if (scenarioSection.content.trim())`. The requirement that follows has a
     real scenario, so the file is otherwise sound. -->
# partial Specification

## Purpose
Exists to hold a scenario heading that was never written out.

## Requirements

### Requirement: Half written
The system SHALL do something.

#### Scenario: Never filled in

#### Scenario: Filled in
- **WHEN** a
- **THEN** b
