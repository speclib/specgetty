## ADDED Requirements

<!--
  tinychange spec delta. Small change != no spec impact — scan the existing
  specs under openspec/specs/ first. Use the right header for what you're doing:
    ## ADDED Requirements      (new behavior)
    ## MODIFIED Requirements   (changed behavior — restate the full requirement)
    ## REMOVED Requirements
  Every requirement needs a SHALL/MUST statement and at least one #### Scenario.
  If the change is a pure refactor with NO behavior change, delete this file and
  set `skip_specs: true` in the change's .openspec.yaml instead.
-->

### Requirement: <what the system guarantees>
The system SHALL <observable behavior>.

#### Scenario: <the case>
- **WHEN** <condition>
- **THEN** <outcome>
